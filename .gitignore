# Build and measurement outputs are reproducible; only sources and results are tracked.
out/
bin/
rust/target/
zig/.zig-cache/
zig/out/
__pycache__/

# CPU and PGO profiles are reproducible measurement artifacts; source and
# summarized results remain tracked while machine-specific samples do not.
prof/*.pprof
bin/
